#from futures.bloomberg.reference import reference

def test_reference():
    pass
    # need to setup a BPipe
    # data = reference(tickers=["ES Comdty"], fields=["FUT_CONT_SIZE"])
    # print(data)
